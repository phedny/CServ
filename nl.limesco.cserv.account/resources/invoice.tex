\documentclass[a4paper,11pt]{memoir}


\usepackage{fullpage}
\usepackage[fleqn]{amsmath}
\usepackage{hyperref}
\usepackage{xspace}
\usepackage{concmath}
\usepackage[T1]{fontenc}
\usepackage{tabularx}


\begin{document}

\begin{flushright}
{\LARGE
Limesco B.V. \\
}
Gildeplein 9 \\
6612 BC  Nederasselt \\
KVK \\
Rekeningnummer \\
E-mailadres \\
\end{flushright}

\begin{flushright}
Factuurnummer: $invoice.id \\
Factuurdatum: $util.formatDate( $invoice.creationDate ) \\
\end{flushright}

\begin{flalign*}
\mbox{Naam}          &: \mbox{$account.fullName.fullName} \\
\mbox{Adres}         &: \mbox{$account.address.streetAddress} \\
\mbox{Postcode}      &: \mbox{$account.address.postalCode} \\
\mbox{Plaats}        &: \mbox{$account.address.locality} \\
\end{flalign*}

\begin{center}
\line(1,0){450}
\end{center}

\newcolumntype{R}{>{\raggedleft\arraybackslash}X}
\begin{tabularx}{\textwidth}{ | X | X | R | R | R | R | }
\hline \\
Omschrijving &    & Aantal  & Stuksprijs & Totaalbedrag & BTW \\
\hline \\
#foreach( $itemLine in $invoice.itemLines ) \\
#if( $itemLine.itemPrice )
$itemLine.description & & $itemLine.itemCount & $util.formatPrice( $invoice.currency, $itemLine.itemPrice ) & $util.formatPrice( $invoice.currency, $itemLine.totalPrice ) & $util.formatTax( $itemLine.taxRate )\% \\
#elseif( $itemLine.pricePerCall )
$itemLine.description & & $itemLine.numberOfCalls + $util.formatDuration( $itemLine.numberOfSeconds ) & $util.formatPrice( $invoice.currency, $itemLine.pricePerCall ) + $util.formatPrice( $invoice.currency, $itemLine.pricePerMinute ) & $util.formatPrice( $invoice.currency, $itemLine.totalPrice ) & $util.formatTax( $itemLine.taxRate )\% \\
#end
#end
\hline
\end{tabularx}

Totaal: $invoice.currency $util.formatPrice( $invoice.currency, $invoice.totalWithoutTaxes )

\newcolumntype{R}{>{\raggedleft\arraybackslash}X}
\begin{tabularx}{\textwidth}{ | X | X | R | }
\hline \\
BTW & Over & BTW-bedrag \\
\hline \\
#foreach( $taxLine in $invoice.taxLines ) \\
$util.formatTax( $taxLine.taxRate )\% & $util.formatPrice( $invoice.currency, $taxLine.baseAmount ) & $util.formatPrice( $invoice.currency, $taxLine.taxAmount ) \\
#end
\hline
\end{tabularx}

Totaal incl. BTW: $invoice.currency $util.formatPrice( $invoice.currency, $invoice.totalWithTaxes )

\end{document}
